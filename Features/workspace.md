# Workspace

## Overview

Workspace is the top-level container in Notelian. Every user belongs to at least one workspace. All pages, databases, members, and settings live inside a workspace.

**Real-world analogy:** A Workspace = your company, team, or personal space. e.g. `Acme Inc`, `Personal Notes`, `Design Team`

---

## User Stories

- As a new user, I want to create a workspace during onboarding so I can start organizing my work immediately.
- As an Admin, I want to invite teammates via email or a shareable link so they can join quickly.
- As an Admin, I want to manage member roles so I can control who can do what.
- As a Member, I want to switch between multiple workspaces I belong to.
- As an Admin, I want to transfer workspace ownership before leaving.
- As an Admin, I want to delete the workspace when it is no longer needed.

---

## Features

### 1. Create Workspace

- Triggered during onboarding or from the workspace switcher
- Required: Workspace Name
- Optional: Workspace Icon (emoji or image upload)
- Creator is automatically assigned the **Admin** role
- On creation: user is taken to the empty workspace ready to create the first page

---

### 2. Edit Workspace

Admin can edit:
- Workspace Name
- Workspace Icon / Avatar
- Workspace URL slug (unique identifier used in URLs)

Changes take effect immediately for all members.

---

### 3. Delete Workspace

- Only the workspace **Admin** (owner) can delete
- Deleting permanently removes: all pages, databases, comments, attachments, and member associations
- Requires confirmation (type workspace name to confirm)
- Cannot be undone

---

### 4. Switch Workspace

- A user can belong to multiple workspaces
- Workspace switcher is accessible from the top of the sidebar
- Displays all workspaces the user is a member of, with their role
- One-click switch, no full page reload

---

### 5. Invite Members

**Via Email:**
- Admin can invite users by email address with a role selected
- Invited user receives an email with a join link (expires in 7 days)
- Pending invites visible in Workspace Settings → Members
- Pending invites can be cancelled before acceptance

**Via Invite Link:**
- Generate a shareable link for the workspace
- Anyone with the link joins as an **Editor** (default role)
- Link can be disabled or regenerated by Admin

---

### 6. Manage Members

Accessible from **Workspace Settings → Members**

- View all members (name, email, role, join date)
- Search / filter members by name or role
- Change a member's role
- Remove a member — their created pages remain but they lose access
- View pending invites and cancel them

---

### 7. Roles & Permissions

| Role | Description |
|------|-------------|
| Admin | Full control — workspace settings, all members, all non-private pages and content. Private pages set by other members are not accessible. Only one Admin (owner) per workspace. |
| Editor | Standard member. Can create and edit pages. Subject to page-level permission restrictions. |
| Viewer | Read-only. Can view pages they have been granted access to. Cannot create or edit content. |

**Role change rules:**
- Admin can change any member's role between Editor and Viewer via the role dropdown
- The Admin role cannot be assigned via the role dropdown — it can only move to another member through the Transfer Ownership flow
- Admin cannot be removed — ownership must be transferred to another member first

---

### 8. Transfer Ownership (Admin Role)

- Only the current Admin can initiate a transfer
- Target user must already be an Editor or Viewer in the workspace
- After transfer: previous Admin becomes an Editor, new Admin gets full control
- Requires email confirmation from the current Admin

---

### 9. Workspace Settings Page

Accessible by Admin only.

**General:**
- Edit name, icon, URL slug

**Members:**
- Invite, manage, remove members
- Manage pending invites

**Security:**
- Disable / regenerate invite link

**Danger Zone:**
- Delete Workspace

---

## Member States

| State | Description |
|-------|-------------|
| Active | Member has accepted invite and has access |
| Invited | Invite sent, not yet accepted |
| Removed | Member was removed, no longer has access |

---

## Data Model

```
Workspace
├── id                  (uuid, primary key)
├── name                (string, required)
├── slug                (string, unique — used in URLs)
├── icon                (string — emoji character or image URL, nullable)
├── created_by          (user_id, foreign key → User)
├── created_at          (timestamp)
└── updated_at          (timestamp)

WorkspaceMember
├── id                  (uuid, primary key)
├── workspace_id        (foreign key → Workspace)
├── user_id             (foreign key → User)
├── role                (enum: admin | editor | viewer)
├── status              (enum: active | invited)
├── invited_by          (user_id, nullable)
├── joined_at           (timestamp, nullable)
└── created_at          (timestamp)
```

---

## API Endpoints

| Method | Endpoint | Description | Access |
|--------|----------|-------------|--------|
| POST | `/api/workspaces` | Create a new workspace | Authenticated user |
| GET | `/api/workspaces` | List workspaces for current user | Authenticated user |
| GET | `/api/workspaces/:id` | Get workspace details | Member+ |
| PATCH | `/api/workspaces/:id` | Update name / icon / slug | Admin |
| DELETE | `/api/workspaces/:id` | Delete workspace | Admin |
| GET | `/api/workspaces/:id/members` | List all members | Member+ |
| POST | `/api/workspaces/:id/members/invite` | Invite by email | Admin |
| PATCH | `/api/workspaces/:id/members/:userId` | Change member role | Admin |
| DELETE | `/api/workspaces/:id/members/:userId` | Remove member | Admin |
| POST | `/api/workspaces/:id/invite-link` | Generate invite link | Admin |
| DELETE | `/api/workspaces/:id/invite-link` | Disable invite link | Admin |
| POST | `/api/workspaces/:id/transfer` | Transfer Admin role | Admin |

---

## UI Screens

| Screen | Route | Access |
|--------|-------|--------|
| Create Workspace (Onboarding) | `/onboarding` | New user |
| Workspace Switcher | Sidebar (global) | All members |
| Workspace Settings — General | `/settings/general` | Admin |
| Workspace Settings — Members | `/settings/members` | Admin |
| Accept Invite | `/invite/:token` | Invited user |

---

## Business Rules

1. Every user must belong to at least one workspace after completing onboarding.
2. A workspace must always have exactly one Admin.
3. Admin role cannot be removed — it can only be transferred to another member.
4. Deleting a workspace is irreversible and requires explicit name confirmation.
5. Invite links default to granting the **Editor** role.
6. Removing a member does not delete their created pages — it only revokes their access.
7. Workspace URL slug must be unique across the platform.
8. Email invites expire after 7 days if not accepted.
9. An unverified user (email not yet confirmed) cannot invite members to a workspace.
10. Guest users (invited to specific pages) are not full workspace members and are listed separately.

---

## Out of Scope (MVP)

- Workspace-level 2FA enforcement
- SSO / SAML
- Audit log export
- Custom domain for workspace
- Workspace templates
